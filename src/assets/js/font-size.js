// unused
var c = ()=>{
    let width = document.documentElement.clientWidth;
    let unit = (20*(width/320)>40+'px'?40:(20*(width/320)+'px'))
    document.documentElement.style.fontSize = n
}
window.addEventListener('load', c)
window.addEventListener('resize', c)
